# Строка подключения к базе данных (async SQLAlchemy).
# Пример для локальной разработки на SQLite:
DSN=sqlite+aiosqlite:///./app.db

# Пример для PostgreSQL:
# DSN=postgresql+asyncpg://user:password@localhost:5432/abc_testing
